package com.car.interfaces;

/**
 * Moveable interface defines a contract for objects that can move.
 * The interface requires the implementation of methods to move, turn left, and turn right.
 */

public interface Moveable {
  void move();
  void turnLeft();
  void turnRight();
}

